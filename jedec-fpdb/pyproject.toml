[project]
name = "jedec-fpdb"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pillow"]

[project.optional-dependencies]
dev = ["pytest", "pytest-xdist"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-n 6"
